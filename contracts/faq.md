## what does delete do !?
And  : delete does not delete the whole mappinh , while it delete the key val;ue pair , that too it does not delete, it sets its value to 0 (it resets the value)

## What is address(0) 
Ans : it is basically ethereum's address 0 , but we use it to represent eth
erc20 token address exist but eth's doesnt 

## safeTransferFrom vs safeBatchTranferFrom !?
Ans : safeTransferFrom : one tokenId and the amount of that token which needs to be send at once
safeBatchTranferFrom : transferring multiple types of tokens at once 
